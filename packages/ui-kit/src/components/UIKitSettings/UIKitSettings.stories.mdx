import { Meta, ArgsTable, Story, Canvas } from '@storybook/addon-docs'
import { useState } from 'react'
import UIKitSettings from '.'

<Meta title="Components/UIKitSettings" component={UIKitSettings} parameters={{ layout: 'centered' }} />

export const Template = args => {
  return <UIKitSettings {...args} />
}

# UIKitSettings

<Canvas withSource="open">
  <Story name="UIKitSettings" args={{ open: true }}>
    {Template.bind({})}
  </Story>
</Canvas>

```
import { UIKitSettings } from '@storystore/ui-kit/components'
```

<ArgsTable story="UIKitSettings" />
