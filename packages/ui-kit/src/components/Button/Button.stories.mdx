import { Meta, ArgsTable, Story, Canvas } from '@storybook/addon-docs'
import Button from '.'
import Icon from 'remixicon/icons/Map/road-map-line.svg'

<Meta title="Components/Button" component={Button} parameters={{ layout: 'centered' }} />

export const Template = args => {
  return <Button {...args} />
}

# Button

Buttons allow users to take actions with a single tap.

<Canvas withSource="open">
  <Story name="Button" args={{ children: 'Button', icon: <Icon /> }}>
    {Template.bind({})}
  </Story>
</Canvas>

```
import { Button } from '@storystore/ui-kit/components'
```

<ArgsTable story="Button" />

## Visual Options

<br />

### Call to Action

<Canvas>
  <Button variant="cta">Hola</Button>
</Canvas>

### Transparent Call to Action

<Canvas>
  <Button variant="cta" transparent>
    Hola
  </Button>
</Canvas>

### Primary

<Canvas>
  <Button variant="primary">Hola</Button>
</Canvas>

### Transparent Primary

<Canvas>
  <Button variant="primary" transparent>
    Hola
  </Button>
</Canvas>

### Secondary

<Canvas>
  <Button variant="secondary">Hola</Button>
</Canvas>

### Transparent Secondary

<Canvas>
  <Button variant="secondary" transparent>
    Hola
  </Button>
</Canvas>
