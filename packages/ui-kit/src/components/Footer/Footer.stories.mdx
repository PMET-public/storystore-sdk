import { Meta, ArgsTable, Story, Canvas } from '@storybook/addon-docs'
import Footer from '.'

import LogoIcon from 'remixicon-react/AppsFillIcon'

<Meta
  title="UIComponents/Footer"
  component={Footer}
  parameters={{
    layout: 'fullscreen',
  }}
/>

export const Template = args => {
  return <Footer logo={<LogoIcon />} {...args} />
}

# Footer

Footers allow users to take actions with a single tap.

<Canvas withSource="open">
  <Story
    name="Footer"
    args={{
      logo: <LogoIcon size={40} />,
      menu: [<a href="#">Articles</a>, <a href="#">Adventures</a>, <a href="#">About</a>, <a href="#">Contacts</a>],
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```
import { Footer } from '@storystore/ui-kit/components'
```
