import { Meta, ArgsTable, Story, Canvas } from '@storybook/addon-docs'
import Footer from '.'
import LogoIcon from 'remixicon/icons/System/apps-fill.svg'

<Meta
  title="Components/Footer"
  component={Footer}
  parameters={{
    layout: 'fullscreen',
  }}
/>

export const Template = args => {
  return (
    <Footer
      logo={<LogoIcon />}
      menu={[<a href="#">Articles</a>, <a href="#">Adventures</a>, <a href="#">About</a>, <a href="#">Contacts</a>]}
      {...args}
    />
  )
}

# Footer

Footers allow users to take actions with a single tap.

<Canvas withSource="open">
  <Story name="Footer">{Template.bind({})}</Story>
</Canvas>

```
import { Footer } from '@storystore/ui-kit/components'
```
