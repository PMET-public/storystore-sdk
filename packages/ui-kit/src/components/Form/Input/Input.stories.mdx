import { Meta, ArgsTable, Story, Canvas } from '@storybook/addon-docs'
import Input from '.'
import { useForm } from 'react-hook-form'

<Meta title="UIComponents/Form/Input" component={Input} parameters={{ layout: 'centered' }} />

export const Template = args => {
  const { register, watch } = useForm()
  return <Input {...args} name="firstName" />
}

# Input

<Canvas withSource="open">
  <Story
    name="Input"
    args={{
      name: 'firstName',
      disabled: false,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```
import { Input } from '@storystore/ui-kit/components'
```
