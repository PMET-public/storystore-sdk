import { Meta, ArgsTable, Story, Canvas } from '@storybook/addon-docs'
import App from '.'
import Header from '../Header'
import Footer from '../Footer'

import LogoIcon from 'remixicon-react/AppsFillIcon'

<Meta
  title="UIComponents/App"
  component={App}
  parameters={{
    layout: 'fullscreen',
  }}
/>

export const Template = args => <App {...args} />

# App

App Shell.

<Canvas withSource="open">
  <Story
    name="App"
    args={{
      header: <Header logo={<LogoIcon />} variant="accent" sticky contained />,
      footer: <Footer logo={<LogoIcon />} contained />,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```
import { App } from '@storystore/ui-kit/components'
```

<ArgsTable story="App" />
