import { Meta, ArgsTable, Story, Canvas } from '@storybook/addon-docs'
import Tile from '.'

<Meta title="UIComponents/Tile" component={Tile} parameters={{ layout: 'centered' }} />

export const Template = args => {
  return <Tile {...args} />
}

# Tile

WIP.

<Canvas withSource="open">
  <Story
    name="Tile"
    args={{
      image: <img src="/__assets/wknd/bg-adventures-1.jpg" width={500} height={500} />,
      heading: <h3>Ahoy-hoy, buckaroos!</h3>,
      subheading: '4-days',
      tags: ['#Flying', '#Get Away'],
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```
import { Tile } from '@storystore/ui-kit/components'
```

<ArgsTable story="Tile" />
