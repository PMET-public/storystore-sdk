import { Meta, ArgsTable, Story, Canvas } from '@storybook/addon-docs'
import Tile from '.'
import imgSrc from '../../experiences/wknd/assets/bg-adventures-1.jpg'
import Image from '../Image'

<Meta title="Components/Tile" component={Tile} />

export const Template = args => {
  return <Tile {...args} />
}

# Tile

WIP.

<Canvas withSource="open">
  <Story
    name="Tile"
    args={{
      image: <Image src={imgSrc} width={500} height={500} />,
      heading: <h3>Ahoy-hoy, buckaroos!</h3>,
      subheading: '4-days',
      tags: ['#Flying', '#Get Away'],
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```
import { Tile } from '@storystore/ui-kit/components'
```

<ArgsTable story="Tile" />
