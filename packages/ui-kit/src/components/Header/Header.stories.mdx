import { Meta, ArgsTable, Story, Canvas } from '@storybook/addon-docs'
import Header from '.'

import LogoIcon from 'remixicon-react/AppsFillIcon'
import HeartIcon from 'remixicon-react/HeartFillIcon'
import CheckInIcon from 'remixicon-react/CheckDoubleLineIcon'

<Meta
  title="UIComponents/Header"
  component={Header}
  parameters={{
    layout: 'fullscreen',
  }}
/>

export const Template = args => <Header {...args} />

export const nav = (
  <nav>
    <a href="#">Menu Item 1</a>
    <a href="#">Menu Item 2</a>
    <a href="#">Menu Item 3</a>
    <a href="#">Menu Item 4</a>
  </nav>
)

# Header

App Header.

<Canvas withSource="open">
  <Story name="Header" args={{ logo: <LogoIcon />, nav }}>
    {Template.bind({})}
  </Story>
</Canvas>

```
import { Header, HeaderNavItem, HeaderNavFill } from '@storystore/ui-kit/components'
```

<ArgsTable story="Header" />

## Visual Options

### Primary

<Canvas>
  <Template variant="primary" logo={<LogoIcon />} nav={nav} />
</Canvas>

### Transparent Primary

<Canvas>
  <Template variant="primary" transparent logo={<LogoIcon />} nav={nav} />
</Canvas>

### Secondary

<Canvas>
  <Template variant="secondary" logo={<LogoIcon />} nav={nav} />
</Canvas>

### Transparent Secondary

<Canvas>
  <Template variant="secondary" transparent logo={<LogoIcon />} nav={nav} />
</Canvas>
