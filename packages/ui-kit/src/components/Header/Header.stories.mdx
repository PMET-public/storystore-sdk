import { Meta, ArgsTable, Story, Canvas } from '@storybook/addon-docs'
import Header, { HeaderMenuItem } from '.'

import LogoIcon from 'remixicon-react/AppsFillIcon'
import HeartIcon from 'remixicon-react/HeartFillIcon'
import CheckInIcon from 'remixicon-react/CheckDoubleLineIcon'

<Meta
  title="UIComponents/Header"
  component={Header}
  parameters={{
    layout: 'fullscreen',
  }}
/>

export const Template = args => <Header {...args} />

export const menu = [
  <HeaderMenuItem variant="fill" />,
  <HeaderMenuItem active>
    <a href="#">Home</a>
  </HeaderMenuItem>,
  <HeaderMenuItem>
    <a href="#">Articles</a>
  </HeaderMenuItem>,
  <HeaderMenuItem>
    <a href="#">Contact</a>
  </HeaderMenuItem>,
  <HeaderMenuItem variant="fill" />,
  <HeaderMenuItem variant="icon" compact>
    <a href="#">
      <HeartIcon aria-label="Likes" />
    </a>
  </HeaderMenuItem>,
  <HeaderMenuItem variant="icon" compact>
    <a href="#">
      <CheckInIcon aria-label="Check-ins" />
    </a>
  </HeaderMenuItem>,
]

# Header

App Header.

<Canvas withSource="open">
  <Story name="Header" args={{ logo: <LogoIcon />, menu }}>
    {Template.bind({})}
  </Story>
</Canvas>

```
import { Header, HeaderNavItem, HeaderNavFill } from '@storystore/ui-kit/components'
```

<ArgsTable story="Header" />

## Visual Options

### Primary

<Canvas>
  <Template variant="primary" logo={<LogoIcon />} menu={menu} />
</Canvas>

### Transparent Primary

<Canvas>
  <Template variant="primary" transparent logo={<LogoIcon />} menu={menu} />
</Canvas>

### Secondary

<Canvas>
  <Template variant="secondary" logo={<LogoIcon />} menu={menu} />
</Canvas>

### Transparent Secondary

<Canvas>
  <Template variant="secondary" transparent logo={<LogoIcon />} menu={menu} />
</Canvas>

### Accent

<Canvas>
  <Template variant="accent" logo={<LogoIcon />} menu={menu} />
</Canvas>

### Transparent Accent

<Canvas>
  <Template variant="accent" transparent logo={<LogoIcon />} menu={menu} />
</Canvas>
