import { Meta, ArgsTable, Story, Canvas } from '@storybook/addon-docs'
import Header, { HeaderMenuItem } from '.'
import LogoIcon from 'remixicon/icons/System/apps-fill.svg'
import HeartIcon from 'remixicon/icons/Health/heart-fill.svg'
import CheckInIcon from 'remixicon/icons/System/check-double-line.svg'

<Meta
  title="Components/Header"
  component={Header}
  parameters={{
    layout: 'fullscreen',
  }}
/>

export const Template = args => <Header {...args} />

# Header

App Header.

<Canvas withSource="open">
  <Story
    name="Header"
    args={{
      logo: <LogoIcon style={{ fill: 'white' }} />,
      menu: [
        <HeaderMenuItem variant="fill" />,
        <HeaderMenuItem active>
          <a href="#">Home</a>
        </HeaderMenuItem>,
        <HeaderMenuItem>
          <a href="#">Articles</a>
        </HeaderMenuItem>,
        <HeaderMenuItem>
          <a href="#">Contact</a>
        </HeaderMenuItem>,
        <HeaderMenuItem variant="fill" />,
        <HeaderMenuItem variant="icon" compact>
          <a href="#">
            <HeartIcon aria-label="Likes" />
          </a>
        </HeaderMenuItem>,
        <HeaderMenuItem variant="icon" compact>
          <a href="#">
            <CheckInIcon aria-label="Check-ins" />
          </a>
        </HeaderMenuItem>,
      ],
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```
import { Header, HeaderNavItem, HeaderNavFill } from '@storystore/ui-kit/components'
```

<ArgsTable story="Header" />

## Visual Options

### Primary

<Canvas>
  <Template variant="primary" logo={<LogoIcon />} />
</Canvas>

### Transparent Primary

<Canvas>
  <Template variant="primary" transparent logo={<LogoIcon />} />
</Canvas>

### Secondary

<Canvas>
  <Template variant="secondary" logo={<LogoIcon />} />
</Canvas>

### Transparent Secondary

<Canvas>
  <Template variant="secondary" transparent logo={<LogoIcon />} />
</Canvas>

### Accent

<Canvas>
  <Template variant="accent" logo={<LogoIcon />} />
</Canvas>

### Transparent Accent

<Canvas>
  <Template variant="accent" transparent logo={<LogoIcon />} />
</Canvas>
