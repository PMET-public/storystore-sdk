import { Meta, ArgsTable, Story, Canvas } from '@storybook/addon-docs'
import Carousel from '.'
import Tile from '../Tile'

<Meta title="Components/Carousel" component={Carousel} parameters={{ layout: 'fullscreen' }} />

export const Template = args => {
  return (
    <Carousel {...args}>
      <Tile
        image={<img src="/__assets/wknd/bg-adventures-1.jpg" width={500} height={500} />}
        heading={<h3>Ahoy-hoy, buckaroos!</h3>}
        subheading="4-days"
        tags={['#Flying', '#Get Away']}
      />
      <Tile
        image={<img src="/__assets/wknd/bg-adventures-1.jpg" width={500} height={500} />}
        heading={<h3>Ahoy-hoy, buckaroos!</h3>}
        subheading="4-days"
        tags={['#Flying', '#Get Away']}
      />
      <Tile
        image={<img src="/__assets/wknd/bg-adventures-1.jpg" width={500} height={500} />}
        heading={<h3>Ahoy-hoy, buckaroos!</h3>}
        subheading="4-days"
        tags={['#Flying', '#Get Away']}
      />
      <Tile
        image={<img src="/__assets/wknd/bg-adventures-1.jpg" width={500} height={500} />}
        heading={<h3>Ahoy-hoy, buckaroos!</h3>}
        subheading="4-days"
        tags={['#Flying', '#Get Away']}
      />
      <Tile
        image={<img src="/__assets/wknd/bg-adventures-1.jpg" width={500} height={500} />}
        heading={<h3>Ahoy-hoy, buckaroos!</h3>}
        subheading="4-days"
        tags={['#Flying', '#Get Away']}
      />
    </Carousel>
  )
}

# Carousel

WIP.

<Canvas withSource="open">
  <Story
    name="Carousel"
    args={{
      show: { sm: 1, lg: 3 },
      gap: 'sm',
      padded: true,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```
import { Carousel } from '@storystore/ui-kit/components'
```

<ArgsTable story="Carousel" />
