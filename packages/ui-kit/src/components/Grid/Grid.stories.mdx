import { Meta, ArgsTable, Story, Canvas } from '@storybook/addon-docs'
import Grid from '.'
import Tile from '../Tile'

<Meta title="Components/Grid" component={Grid} />

export const Template = args => {
  return (
    <Grid {...args}>
      <Tile
        image={<img src="/__assets/wknd/bg-adventures-1.jpg" width={500} height={500} />}
        heading={<h3>Ahoy-hoy, buckaroos!</h3>}
        subheading="4-days"
        tags={['#Flying', '#Get Away']}
      />
      <Tile
        image={<img src="/__assets/wknd/bg-adventures-1.jpg" width={500} height={500} />}
        heading={<h3>Ahoy-hoy, buckaroos!</h3>}
        subheading="4-days"
        tags={['#Flying', '#Get Away']}
      />
      <Tile
        image={<img src="/__assets/wknd/bg-adventures-1.jpg" width={500} height={500} />}
        heading={<h3>Ahoy-hoy, buckaroos!</h3>}
        subheading="4-days"
        tags={['#Flying', '#Get Away']}
      />
      <Tile
        image={<img src="/__assets/wknd/bg-adventures-1.jpg" width={500} height={500} />}
        heading={<h3>Ahoy-hoy, buckaroos!</h3>}
        subheading="4-days"
        tags={['#Flying', '#Get Away']}
      />
      <Tile
        image={<img src="/__assets/wknd/bg-adventures-1.jpg" width={500} height={500} />}
        heading={<h3>Ahoy-hoy, buckaroos!</h3>}
        subheading="4-days"
        tags={['#Flying', '#Get Away']}
      />
    </Grid>
  )
}

# Grid

WIP

<Canvas withSource="open">
  <Story args={{ columns: { sm: '1fr', lg: '1fr 1fr 1fr' }, gap: 'sm' }} name="Grid">
    {Template.bind({})}
  </Story>
</Canvas>

```
import { Grid } from '@storystore/ui-kit/components'
```
