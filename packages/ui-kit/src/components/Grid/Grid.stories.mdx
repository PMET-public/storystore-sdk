import { Meta, ArgsTable, Story, Canvas } from '@storybook/addon-docs'
import Grid from '.'
import Tile from '../Tile'
import Image from '../Image'
import imgSrc from '../../experiences/wknd/assets/bg-adventures-1.jpg'

<Meta title="Components/Grid" component={Grid} />

export const Template = args => {
  return (
    <Grid {...args}>
      <Tile
        image={<Image src={imgSrc} width={500} height={500} />}
        heading={<h3>Ahoy-hoy, buckaroos!</h3>}
        subheading="4-days"
        tags={['#Flying', '#Get Away']}
      />
      <Tile
        image={<Image src={imgSrc} width={500} height={500} />}
        heading={<h3>Ahoy-hoy, buckaroos!</h3>}
        subheading="4-days"
        tags={['#Flying', '#Get Away']}
      />
      <Tile
        image={<Image src={imgSrc} width={500} height={500} />}
        heading={<h3>Ahoy-hoy, buckaroos!</h3>}
        subheading="4-days"
        tags={['#Flying', '#Get Away']}
      />
      <Tile
        image={<Image src={imgSrc} width={500} height={500} />}
        heading={<h3>Ahoy-hoy, buckaroos!</h3>}
        subheading="4-days"
        tags={['#Flying', '#Get Away']}
      />
      <Tile
        image={<Image src={imgSrc} width={500} height={500} />}
        heading={<h3>Ahoy-hoy, buckaroos!</h3>}
        subheading="4-days"
        tags={['#Flying', '#Get Away']}
      />
    </Grid>
  )
}

# Grid

WIP

<Canvas withSource="open">
  <Story args={{ columns: { small: '1fr', large: '1fr 1fr 1fr' }, gap: 'sm' }} name="Grid">
    {Template.bind({})}
  </Story>
</Canvas>

```
import { Grid } from '@storystore/ui-kit/components'
```
