import { Meta, ArgsTable, Story, Canvas } from '@storybook/addon-docs'
import Banner from '.'
import Image from '../Image'
import Button from '../Button'
import Heading from '../Heading'
import backgroundImage from '../../experiences/wknd/assets/bg-adventures-1.jpg'

<Meta title="Components/Banner" component={Banner} />

export const Template = args => {
  return <Banner {...args} />
}

# Banner

App Banner.

<Canvas withSource="open">
  <Story
    name="Banner"
    args={{
      textColor: 'black',
      backgroundImage: <Image src={backgroundImage} />,
      heading: <Heading size={{ small: '2xl', large: '5xl' }}>Banner heading</Heading>,
      button: <Button variant="cta">Button</Button>,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```
import { Banner } from '@storystore/ui-kit/components'
```

<ArgsTable story="Banner" />

## Visual Options

### Center

<Canvas>
  <Banner
    backgroundColor="teal"
    heading={<Heading size="2xl">Banner heading</Heading>}
    button={
      <Button variant="accent" transparent>
        Button
      </Button>
    }
    align="center"
  />
</Canvas>

### Left

<Canvas>
  <Banner
    backgroundColor="teal"
    heading={<Heading size="2xl">Banner heading</Heading>}
    button={
      <Button variant="accent" transparent>
        Button
      </Button>
    }
    align="left"
  />
</Canvas>

### Right

<Canvas>
  <Banner
    backgroundColor="teal"
    heading={<Heading size="2xl">Banner heading</Heading>}
    button={
      <Button variant="accent" transparent>
        Button
      </Button>
    }
    align="right"
  />
</Canvas>
