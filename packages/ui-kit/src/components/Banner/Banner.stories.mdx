import { Meta, ArgsTable, Story, Canvas } from '@storybook/addon-docs'
import Banner from '.'
import Image from '../Image'
import Button from '../Button'
import backgroundImage from '../../experiences/wknd/assets/background-header.jpg'

<Meta title="Components/Banner" component={Banner} />

export const Template = args => {
  return <Banner {...args} />
}

# Banner

App Banner.

<Canvas withSource="open">
  <Story
    name="Banner"
    args={{
      textColor: 'black',
      backgroundImage: <Image src={backgroundImage} />,
      title: <h2>Banner Title</h2>,
      button: <Button variant="cta">Button</Button>,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```
import { Banner } from '@storystore/ui-kit/components'
```

<ArgsTable story="Banner" />

## Visual Options

### Center

<Canvas>
  <Banner
    backgroundColor="teal"
    title={<h2>Banner Title</h2>}
    button={
      <Button variant="accent" transparent>
        Button
      </Button>
    }
    align="center"
  />
</Canvas>

### Left

<Canvas>
  <Banner
    backgroundColor="teal"
    title={<h2>Banner Title</h2>}
    button={
      <Button variant="accent" transparent>
        Button
      </Button>
    }
    align="left"
  />
</Canvas>

### Right

<Canvas>
  <Banner
    backgroundColor="teal"
    title={<h2>Banner Title</h2>}
    button={
      <Button variant="accent" transparent>
        Button
      </Button>
    }
    align="right"
  />
</Canvas>
