import { Meta, ArgsTable, Story, Canvas } from '@storybook/addon-docs'
import Banner from '.'
import Button from '../Button'
import Heading from '../Heading'

<Meta title="UIComponents/Banner" component={Banner} parameters={{ layout: 'centered' }} />

export const Template = args => {
  return <Banner {...args} />
}

# Banner

App Banner.

<Canvas withSource="open">
  <Story
    name="Banner"
    args={{
      width: '100vh',
      height: '50vh',
      textColor: 'black',
      backgroundImage: <img src="/__assets/wknd/bg-adventures-1.jpg" />,
      heading: <Heading size={{ sm: '2xl', lg: '5xl' }}>Banner heading</Heading>,
      button: <Button variant="cta">Button</Button>,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```
import { Banner } from '@storystore/ui-kit/components'
```

<ArgsTable story="Banner" />

## Visual Options

<br />

### Center

<Canvas>
  <Banner
    backgroundColor="teal"
    heading={<Heading size="2xl">Banner heading</Heading>}
    button={
      <Button variant="accent" transparent>
        Button
      </Button>
    }
    align="center"
  />
</Canvas>

### Left

<Canvas>
  <Banner
    backgroundColor="teal"
    heading={<Heading size="2xl">Banner heading</Heading>}
    button={
      <Button variant="accent" transparent>
        Button
      </Button>
    }
    align="left"
  />
</Canvas>

### Right

<Canvas>
  <Banner
    backgroundColor="teal"
    heading={<Heading size="2xl">Banner heading</Heading>}
    button={
      <Button variant="accent" transparent>
        Button
      </Button>
    }
    align="right"
  />
</Canvas>
