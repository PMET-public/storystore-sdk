import { Meta, Source } from '@storybook/addon-docs'

<Meta title="Theme/Tokens" />

export const Color = ({ variant = 'surface', ...props }) => (
  <div
    {...props}
    style={{
      padding: '1rem',
      background: `var(--color-${variant})`,
      color: `var(--color-on-${variant})`,
      ...props.style,
    }}
  />
)

# Theme

With `MDX`, we can define a story for `Checkbox` right in the middle of our
Markdown documentation.

<Source
  language="css"
  code={`
    :root {
    --color: #fff;
}
  `}
></Source>

## Colors

<div
  style={{
    display: 'grid',
    gridTemplateColumns: '1fr 1fr 1fr',
    gridGap: '1rem',
    padding: '1rem',
    background: 'var(--color-surface)',
    color: 'var(--color-on-surface)',
  }}
>
  <Color variant="surface-lighter">Surface (lighter)</Color>
  <Color variant="surface">Surface</Color>
  <Color variant="surface-darker">Surface (darker)</Color>
  <Color variant="accent-lighter">Accent (lighter)</Color>
  <Color variant="accent">Accent</Color>
  <Color variant="accent-darker">Accent (darker)</Color>
  <Color variant="primary-lighter">Primary (lighter)</Color>
  <Color variant="primary">Primary</Color>
  <Color variant="primary-darker">Primary (darker)</Color>
  <Color variant="secondary-lighter">Secondary (lighter)</Color>
  <Color variant="secondary">Secondary</Color>
  <Color variant="secondary-darker">Secondary (darker)</Color>
</div>
