import { Meta, Story, Canvas } from '@storybook/addon-docs'
import Home, { HOME_AEM_MODEL_PAGE_PATH } from '.'
import { App } from '../../components'
import { settings } from '../../../../../.storybook/lib'

<Meta
  title="Experiences/WKND/Pages/Home"
  component={Home}
  parameters={{
    layout: 'fullscreen',
    title: 'Settings',
    variables: { ...settings },
  }}
/>

export const Template = args => (
  <App pagePath={HOME_AEM_MODEL_PAGE_PATH}>
    <Home {...args} />
  </App>
)

# Home

<Canvas withSource="open">
  <Story name="Home">{Template.bind({})}</Story>
</Canvas>
