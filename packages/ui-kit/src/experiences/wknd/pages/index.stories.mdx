import { Meta, Story, Canvas } from '@storybook/addon-docs'
import Home, { HOME_QUERY } from '.'
import App from './_app'

<Meta
  title="Experiences/WKND/Home"
  component={Home}
  parameters={{
    layout: 'fullscreen',
    title: 'Settings',
    variables: {
      fields: {
        graphQlEndpoint: {
          label: 'GraphQL URL',
          placeholder: 'https://...',
          defaultValue: 'http://localhost:4502/content/graphql/global/endpoint.json',
        },
        graphQlBasicAuth: {
          label: 'GraphQL Auth',
          placeholder: 'username:password',
          defaultValue: 'admin:admin',
        },
      },
    },
  }}
/>

export const Template = args => (
  <App>
    <div style={{ height: '200vh' }}>
      <Home {...args} />
    </div>
  </App>
)

# Home

<Canvas withSource="open">
  <Story name="Home" args={{}}>
    {Template.bind({})}
  </Story>
</Canvas>
