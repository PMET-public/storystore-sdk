import { Meta, Story, Canvas } from '@storybook/addon-docs'
import Adventures from '.'
import { App } from '../../components'
import { settings } from '../../../../../.storybook/lib'

<Meta
  title="Experiences/WKND/Pages/Adventures"
  component={Adventures}
  parameters={{
    layout: 'fullscreen',
    title: 'Settings',
    variables: { ...settings },
  }}
/>

export const Template = args => (
  <App>
    <Adventures {...args} />
  </App>
)

# Adventures

<Canvas withSource="open">
  <Story name="Adventures" args={{}}>
    {Template.bind({})}
  </Story>
</Canvas>
