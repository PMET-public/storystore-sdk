{
  "name": "@storystore/sdk",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/toolbox",
    "packages/storybook-variables",
    "packages/ui-kit",
    "packages/next-apollo",
    "packages/pwa"
  ],
  "devDependencies": {
    "concurrently": "^6.2.0",
    "prettier": "^2.3.1"
  },
  "scripts": {
    "cleanup": "yarn workspaces run cleanup",
    "prepare": "yarn workspaces run prepare",
    "sdk": "concurrently --names \"UIKit,WKND\" \"yarn workspace @storystore/ui-kit dev\" \"yarn workspace @storystore/pwa dev\"",
    "pkg": "concurrently --names \"UIKit,WKND\" \"yarn workspace @storystore/ui-kit dev\" \"yarn workspace @storystore/pwa dev\""
  },
  "engines": {
    "node": ">=12.0.0"
  }
}
