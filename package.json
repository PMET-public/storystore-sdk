{
  "private": true,
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "devDependencies": {
    "prettier": "^2.3.1"
  },
  "scripts": {
    "prepare": "yarn workspaces run prepare",
    "cleanup": "yarn workspaces run cleanup"
  }
}
