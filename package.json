{
  "private": true,
  "workspaces": [
    "packages/*",
    "pwa/*"
  ],
  "devDependencies": {
    "prettier": "^2.3.1"
  },
  "scripts": {
    "uikit": "yarn workspace @storystore/ui-kit",
    "prepare": "yarn workspaces run prepare",
    "cleanup": "yarn workspaces run cleanup"
  }
}
